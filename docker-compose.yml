version: '3.9'

services:        
  app:
    hostname: app
    container_name: penugasan-1-netics
    build:
      context: . 
      dockerfile: Dockerfile
    ports:
      - ${GOLANG_PORT}:8888
    restart: always
    env_file:
      - .env